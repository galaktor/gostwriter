language: go

go:
  - 1.2
  - 1.3
  - tip

before_install:
  - lsmod
  - sudo apt-get update
  - sudo apt-get install kernel
  - sudo modprobe uinput
  - lsmod
  - chmod +0666 /dev/uinput

install:
  - go get -d -v ./...